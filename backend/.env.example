# Server port number (number, no quotes needed)
PORT=4000

# MongoDB connection string (string — wrap in quotes because it contains special characters)
MONGO_URI="your_mongodb_connection_string_here"

# JWT secret key (string — wrap in quotes)
JWT_SECRET="your_strong_jwt_secret_here"
